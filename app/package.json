{
  "name": "cars-zuria-baileys-app-pro",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "prebuild": "node -e \"console.log('[prebuild] node', process.version)\""
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.9",
    "fastify": "^4.27.2",
    "pino": "^9.3.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.2",
    "rimraf": "^6.0.1"
  }
}
