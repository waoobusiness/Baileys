services:
  - type: web
    name: zuria-wa
    env: node
    plan: starter
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    autoDeploy: true
    envVars:
      - key: API_KEY
        sync: false
      - key: DATA_DIR
        value: /data
      - key: LOG_LEVEL
        value: info
      # Décommente uniquement si tu as un vrai proxy HTTP/HTTPS
      # - key: WS_PROXY_URL
      #   value: "http://user:pass@proxy.example.com:8080"
    disk:
      name: wa-data
      mountPath: /data
      sizeGB: 1
