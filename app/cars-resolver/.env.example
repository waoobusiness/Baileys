# HTTP
PORT=3002
RESOLVER_BEARER=change-me-long-random        # Bearer à vérifier côté Lovable

# Supabase (service key car on écrit/merge)
SUPABASE_URL=https://<project>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=xxxxx

# Scraping
DEFAULT_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122 Safari/537.36
REQUEST_BACKOFF_MS_MIN=300
REQUEST_BACKOFF_MS_MAX=700
MAX_PAGES=60                                  # sécurité pagination

# Import → DB
UPSERT_TO_SUPABASE=true
MARK_MISSING_AS_UNLISTED=true                 # marquer "disparu du site" → unlisted

# OpenAI (OPTIONNEL, activable plus tard)
OPENAI_API_KEY=sk-...
VS_AUTO_SYNC=false                            # true pour pousser 1 doc / véhicule
VS_PREFIX=cars-
