# Render sets PORT automatically; 3000 for local dev
PORT=3000

# Log verbosity: trace | debug | info | warn | error
LOG_LEVEL=info

# Persistent disk path on Render (mount the disk at /data)
DATA_DIR=/data/wa-auth

# Auto-download received media (1=yes, 0=no)
AUTO_DOWNLOAD_MEDIA=1

# Simple echo to test delivery on incoming text (1=yes)
ECHO_REPLY=0
