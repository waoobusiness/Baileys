# ---- Gateway config ----
PORT=10000
LOG_LEVEL=info
DATA_DIR=./data/wa-auth

# optional API auth (recommandé)
AUTH_TOKEN=REPLACE_ME

# si 1: enrichissements médias (facultatif)
AUTO_DOWNLOAD_MEDIA=0
