# --- Runtime ---
PORT=3000
LOG_LEVEL=info
TZ=Europe/Zurich

# --- Persistence (Render Disk monté sur /data) ---
DATA_DIR=/data/wa-auth

# --- Features ---
AUTO_DOWNLOAD_MEDIA=1
ECHO_REPLY=0

# --- API security (protège /contacts, /chats, /messages, /send-*) ---
# Si vide, pas d’auth. En prod: mets un token fort et envoie "Authorization: Bearer <token>"
AUTH_TOKEN=

# --- Webhooks sortants (vers Lovable / une API maison) ---
# Si WEBHOOK_OUTBOX_URL est défini, on POSTe chaque event (qr/status/sync/message) vers cette URL.
# La signature HMAC (sha256) du body est envoyée dans X-Signature si WEBHOOK_SIGNING_SECRET est défini.
WEBHOOK_OUTBOX_URL=
WEBHOOK_SIGNING_SECRET=

# --- Supabase (pré-provision, pas encore utilisés dans le code) ---
SUPABASE_URL=
SUPABASE_ANON_KEY=
# NOTE: La clé service_role NE DOIT PAS être exposée côté client.
SUPABASE_SERVICE_ROLE=
SUPABASE_JWT_SECRET=
SUPABASE_SCHEMA=public

# Nommage des tables (optionnel, utile quand Lovable branchera la DB)
SUPABASE_CONTACTS_TABLE=wa_contacts
SUPABASE_CHATS_TABLE=wa_chats
SUPABASE_MESSAGES_TABLE=wa_messages
SUPABASE_MEDIA_TABLE=wa_media
